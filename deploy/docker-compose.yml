services:
  # Docker named volumes are created with root ownership and don't inherit
  # permissions from the image's mount point. The app container runs as UID 1000
  # and would otherwise fail to write to /data/dpkeys or /data/uploads.
  init-perms:
    image: alpine:3
    container_name: ordowiki-init-perms
    restart: "no"
    user: root
    command: ["sh", "-c", "chown -R 1654:1654 /data/dpkeys /data/uploads && chmod 700 /data/dpkeys"]
    volumes:
      - dpkeys:/data/dpkeys
      - uploads:/data/uploads

  migrator:
    image: ghcr.io/${GHCR_OWNER}/ordowiki:${IMAGE_TAG:-latest}
    container_name: ordowiki-migrator
    restart: "no"
    command: ["dotnet", "OrdoWiki.Web.dll", "--migrate"]
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=ordowiki;Username=ordowiki;Password=${DB_PASSWORD}"
    depends_on:
      postgres:
        condition: service_healthy
    networks: [internal]

  app:
    image: ghcr.io/${GHCR_OWNER}/ordowiki:${IMAGE_TAG:-latest}
    container_name: ordowiki-app
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=ordowiki;Username=ordowiki;Password=${DB_PASSWORD}"
      UploadsRoot: /data/uploads
      DataProtection__KeysPath: /data/dpkeys
      Admin__Username: ${ADMIN_USERNAME}
      Admin__InitialPassword: ${ADMIN_INITIAL_PASSWORD}
    volumes:
      - uploads:/data/uploads
      - dpkeys:/data/dpkeys
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
      init-perms:
        condition: service_completed_successfully
    networks: [internal]

  postgres:
    image: postgres:16
    container_name: ordowiki-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ordowiki
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ordowiki
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ordowiki -d ordowiki"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks: [internal]

  caddy:
    image: caddy:2
    container_name: ordowiki-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on: [app]
    networks: [internal]

volumes:
  pgdata:
  uploads:
  dpkeys:
  caddy_data:
  caddy_config:

networks:
  internal:
    driver: bridge
